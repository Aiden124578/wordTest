<view class="wronglist">
  <view class="top">
    <view class="top-col">不认识的单词</view>
  </view>
  <view class="items" wx:for="{{resultData}}" wx:key="*this">
    <view class="item-col word">{{item.item}}</view>
    <view class="item-col">{{item.data}}</view>
  </view>
</view>